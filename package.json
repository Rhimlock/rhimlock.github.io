{
  "name": "tstest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" ",
    "build": "tsc -p tsconfig.json ",
    "serve": "tsc -p tsconfig.json --watch",
    "pretty": "prettier --write \"./**/*.{js,jsx,mjs,cjs,ts,tsx,json}\"",
    "build:rust": "cd rust && cargo build && wasm-pack build --dev --target web && cd .."
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
