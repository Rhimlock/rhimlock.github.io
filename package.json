{
  "name": "tstest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./js tsc && tsc -p tsconfig.json",
    "watch": "rm -rf ./js tsc && tsc -p tsconfig.json --watch"
  },
  "author": "",
  "license": "ISC",
  "type": "module"
}
